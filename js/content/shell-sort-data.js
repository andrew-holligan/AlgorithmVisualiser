export const shellSortData = new Map();

shellSortData.set("javascript", ``);
shellSortData.set("java", ``);
shellSortData.set("python", ``);
shellSortData.set("cpp", `STILL TO IMPLEMENT`);
shellSortData.set("csharp", `STILL TO IMPLEMENT`);
shellSortData.set("c", `STILL TO IMPLEMENT`);
